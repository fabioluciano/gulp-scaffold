(function () {
  'use strict';

  var gulp             = require('gulp'),
      dependencies     = require('main-bower-files'),
      plugin           = require('gulp-load-plugins')({
        lazy: true,
        camelize: true
      });

}());
